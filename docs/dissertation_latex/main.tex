%==============================================================================
% !!! FROZEN -- NOT THE LIVE COPY (as of 2026-07-16) !!!
% Drafting is happening in  draft.tex  (single file). This file and chapters/*.tex
% are out of date until draft.tex is split back at its "% ===== FILE:" markers.
% Do not edit chapters/*.tex meanwhile -- edits there will be overwritten.
%==============================================================================
% MSc Dissertation -- main document
% EPCC, University of Edinburgh
%
% Working title: Hybrid MPI+OpenMP Performance of PETSc Krylov Solvers on ARCHER2
%
% Compile with EITHER engine (both work from this single source):
%   tectonic main.tex          <- recommended (auto-fetches packages, self-cleans)
%   latexmk -pdf main.tex      <- pdflatex path
%   latexmk -xelatex main.tex  <- xelatex path (after `sudo apt install texlive-xetex`)
%
% NOTE: this is a plain, portable skeleton. When the official EPCC dissertation
% template arrives (see 00_outline gap list, C-class), swap \documentclass and
% the title block for the template's and keep the chapters/ files as-is.
%==============================================================================
% Draft setting: `oneside` = no forced blank pages between chapters.
% For the bound/printed submission, switch to [11pt,a4paper,twoside,openright]
% and re-add bindingoffset to geometry below.
\documentclass[11pt,a4paper,oneside]{report}

%------------------------------------------------------------------ engine-safe fonts
\usepackage{iftex}
\ifPDFTeX
  \usepackage[utf8]{inputenc}
  \usepackage[T1]{fontenc}
  \usepackage{lmodern}
\else
  % XeTeX / LuaTeX / tectonic
  \usepackage{fontspec}
  % \setmainfont{TeX Gyre Termes} % uncomment once a system font is chosen
\fi

%------------------------------------------------------------------ layout & graphics
\usepackage[a4paper,margin=2.5cm]{geometry}
\usepackage{graphicx}
\graphicspath{{figures/}}
\usepackage{booktabs}
\usepackage{amsmath,amssymb}
\usepackage{listings}   % for code/config snippets
\usepackage{microtype}
\usepackage{csquotes}

%------------------------------------------------------------------ bibliography
\usepackage[backend=biber,style=numeric-comp,sorting=none]{biblatex}
\addbibresource{references.bib}

%------------------------------------------------------------------ links (load late)
\usepackage[hidelinks]{hyperref}
\usepackage[capitalise]{cleveref}   % must come after hyperref

% absorb the odd overfull line from long monospace flags (draft-friendly)
\emergencystretch=2em

%------------------------------------------------------------------ title metadata
\title{Hybrid MPI+OpenMP Performance of PETSc Krylov Solvers on ARCHER2}
\author{Leyan Li}
\date{August 2026}

\begin{document}

%============================================================ front matter
\pagenumbering{roman}
\input{chapters/00_frontmatter}
\tableofcontents
% Re-enable once figures/tables exist — until then these are near-empty pages.
% \listoffigures
% \listoftables
\clearpage

%============================================================ main matter
\pagenumbering{arabic}
\include{chapters/ch1_introduction}
\include{chapters/ch2_background}
\include{chapters/ch3_methodology}
\include{chapters/ch4_singlenode}
\include{chapters/ch5_multinode}
\include{chapters/ch6_profiling}
\include{chapters/ch7_discussion}
\include{chapters/ch8_conclusion}

%============================================================ back matter
\printbibliography[heading=bibintoc]

\appendix
\include{chapters/appendix}

\end{document}
